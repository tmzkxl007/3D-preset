# 3D 프리셋 (두둥_픽) — 이 저장소 규칙

세로 숏폼 채널 "두둥_픽" 의 편집 규격이다. 스타일 판단은 전부 `3D.md` 가 기준이고,
이 파일은 **경계**만 정한다.

## 이 저장소의 것

- 캔버스 **1080×1920 세로**, 길이 **25~45초**(45초에 가깝게), 30fps
- 자산은 `assets/` 안의 것만 쓴다 — 두둥픽 로고 PNG, 두둥 북소리, NanumSquareRoundB, 레코체, `assets/sfx/`(합성)
- 파이프라인은 `scripts/narration_variant/` — 작업 폴더(`~/3d_works/<영상id>`)로 **복사해서** 대본만 갈아끼운다. 저장소 안에서 직접 렌더하지 않는다
- 완성본은 `내 문서\두둥픽\` 으로 내보낸다
- 새 영상을 만들기 전에 `3D.md` 를 처음부터 읽는다. 특히 5(문체)·13(인물 매칭)·14(크롭)·16(제목)·17(효과음)번

## 이 저장소의 것이 아닌 것

**크랩 프리셋(`~/크랩-preset`)의 규격·자산·대본을 여기 들이지 않는다.** 크랩은 가로 1920×1080에
6분 30초짜리 뉴스 설명 미드폼이라 여기와 공통점이 없다. 크랩 관련 변경은 그 저장소에서 한다.

`~/.claude/preset_firewall.py` 가 두 저장소를 동시에 건드리는 명령을 실행 전에 막는다.
차단되면 명령을 나눠서 실행하고, 의도적 교차 작업이면 `FIREWALL_OK` 를 명령에 넣는다.

## 납품 전

```
python preset_guard.py "<완성본.mp4>"
```

해상도·길이·라우드니스·납품 경로를 `3D.preset.json` 과 대조한다. 실패하면 고치고 다시 돌린다.

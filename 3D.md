# "3D" 프리셋 — 두둥_픽 스타일 지침서

> 출처: CapCut 초안 `260624-b094` (채널 "두둥_픽") 역분석.
> 이 문서는 볼케이노 서버의 공식 프리셋이 아니라, 사용자가 CapCut에서 직접 편집해 온
> 개인 스타일을 재현 가능하도록 정리한 지침서다. 새 영상을 만들 때 이 문서를 그대로
> 따르면 기존 "3D" 시리즈와 같은 톤이 나온다.

## 1. 소재
- 3D 게임엔진 렌더링 스타일의 스톡/재연 영상(예: Afrimax류 채널 영상)을 소스로 쓴다.
- 원본 영상을 여러 구간으로 잘라 하나의 타임라인에 순서대로 배치한다(컷마다 별도 소스클립일 필요는 없고, 같은 원본의 여러 구간이어도 된다).
- **컷 사이에 CapCut 트랜지션 효과를 넣지 않는다** — 하드컷만 쓴다. 속도감은 재생속도와 사운드로 만든다.

## 2. 캔버스 · 재생속도
- 캔버스: **1080×1920** (세로 9:16), `ratio: original`.
- 모든 비디오 클립의 재생속도: **1.2배속** (`speed: 1.2`, `mode: 0`).
- 클립마다 세로 위치(`clip.transform.y`)를 손으로 미세 조정한다 — 화면 중요 피사체가 자막 밴드에 가리지 않도록 클립마다 다른 값(예: -0.45, -0.05 등)을 준다. 고정 공식은 없고 컷마다 눈으로 보고 맞춘다.
- 스케일은 그대로 둔다(`scale: 1.0`, `uniform_scale.on: true`) — 확대하지 않는다.

## 3. 색보정
- CapCut 필터 **"훈훈한 포근함"**(따뜻한 톤, effect_id `7535020867344928061`)을 전체 영상에 한 번 적용, 강도 **약 0.30**(`value ≈ 0.302`).
- 추가로 클립마다 CapCut의 스마트 컬러 조정(`smart_color_adjust`, effect_id `7501974767453474064`)을 강도 **약 0.30**(`value ≈ 0.298`)으로 얹는다.
- 즉 "은은한 따뜻한 필터 + 자동 보정 약하게" 조합이며, 강하게 튀는 색보정은 쓰지 않는다.

## 4. 자막(본문 캡션)
- 폰트: **NanumSquareRoundB.ttf**(나눔스퀘어라운드 Bold).
- 크기: 15 (CapCut 내부 스케일 기준).
- 채우기색: 흰색(`#ffffff`).
- 외곽선(stroke): 검정, 두께 ≈ 0.074.
- 그림자: 검정, 불투명도 0.9, 거리 5, 각도 -45°, 부드러움(smoothing) 0.45.
- 정렬: 가운데(`alignment: 1`), 줄간격 `line_spacing: 0.17`.
- 문장을 통째로 한 자막에 넣지 않는다 — **한 호흡(2~6어절)마다 잘라 다음 자막으로 넘긴다.** 실측 예시:
  - `0.00s +0.93s` "한 여자가 매주"
  - `0.93s +1.13s` "고급 자전거를 타고"
  - `2.07s +1.00s` "국경을 넘어 다녔습니다"
  - 평균 컷당 0.8~1.2초, 한 편에 캡션 35~40장 내외.
- 나레이션(TTS) 없음 — **자막만으로 이야기를 끌고 간다.**

## 5. 제목(후킹 문구)
- 폰트: **Recipekorea 레코체**(손글씨 느낌의 한글 폰트).
- 크기: 14, 채우기색: 검정.
- 위치: 화면 상단, **영상 처음부터 끝까지 고정 노출**(전체 길이 동안 한 텍스트 세그먼트).
- 내용: 이야기의 결말/교훈을 미리 보여주는 한두 줄짜리 문구(예: "눈앞의 진실을 두고도 / 의심에 눈이 멀어버리는 순간"). 클릭베이트식으로 반전을 먼저 던지는 방식이다.

## 6. 워터마크 / 채널 브랜딩
- 좌상단에 채널 로고 오버레이(투명 배경 PNG, 1080×1920 풀캔버스, 전체 길이 고정):
  - 검정 폭발/별 모양 배지 아이콘 + 그 안에 노란-주황 그라데이션으로 "두둥" 글자.
  - 오른쪽에 검정 글씨로 채널명 "두둥_픽" 워드마크.
- 화면 하단에 옅은 흰색→회색 그라데이션 비네트가 살짝 깔려 있다(자막 가독성 보조용으로 추정).

## 7. 오디오
- **TTS 나레이션 없음.**
- 배경음악 한 곡(약 53초 분량)을 반복 재생하지 않고, **각 자막 구간 길이에 맞춰 곡의 서로 다른 구간을 잘라 붙인다** — 즉 BGM 편집점이 자막 컷과 정확히 맞물리도록 손으로 잘게 잘라 배치한다(단순 반복 루프가 아님).
- 영상 시작 지점(0~1.8초)에 **"두둥" 드럼 히트 효과음**을 한 번 깔아 오프닝을 강하게 연다.
- 배경음악 볼륨은 1.0(별도 더킹 없음 — 나레이션이 없으므로).

## 8. 필요한 리소스 체크리스트 (다른 PC에서 재현 시)
- 폰트 설치 필요: `NanumSquareRoundB.ttf`(나눔스퀘어라운드 Bold, 눈누/네이버 배포), `Recipekorea 레코체 FONT.ttf`(레시피코리아 배포 무료 폰트).
- CapCut 내장 리소스라 그대로 복제 불가능한 것: 필터 "훈훈한 포근함"(effect_id 7535020867344928061), 스마트 컬러 조정(effect_id 7501974767453474064) — CapCut에서 이름으로 검색해 적용해야 한다.
- 채널 워터마크 PNG(`탬플릿.png`, 두둥_픽 로고): 별도로 보관해 두었다가 매 영상 오버레이 트랙에 재사용.
- 두둥 드럼 효과음(`두둥 북소리.MP3`): 별도로 보관해 재사용.

## 9. 볼케이노 파이프라인과의 관계
- 볼케이노의 `인물형` 프리셋과 뼈대(원본 영상 재활용 + 자막 스토리텔링 + 나레이션 없음)는 같지만, 다음이 다르다:
  - 인물형: 흰 캔버스 위 1080×797 비디오 + 상하 텍스트 밴드(명사형 종결 자막, 노란 인용구 하이라이트).
  - 3D(두둥_픽): 풀프레임 비디오(1080×1920) 위에 자막을 얹고, 컬러 필터·워터마크·오프닝 드럼히트·1.2배속을 추가한 스타일.
- 볼케이노로 초안을 뽑은 뒤 이 문서의 4~7번 항목을 CapCut에서 수동으로 얹으면 "두둥_픽" 톤이 재현된다.

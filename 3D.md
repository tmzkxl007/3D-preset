# "3D" 프리셋 — 두둥_픽 스타일 지침서

> 출처: CapCut 초안 `260624-b094` (채널 "두둥_픽") 역분석.
> 이 문서는 볼케이노 서버의 공식 프리셋이 아니라, 사용자가 CapCut에서 직접 편집해 온
> 개인 스타일을 재현 가능하도록 정리한 지침서다. 새 영상을 만들 때 이 문서를 그대로
> 따르면 기존 "3D" 시리즈와 같은 톤이 나온다.

## 1. 소재
- 3D 게임엔진 렌더링 스타일의 스톡/재연 영상(예: Afrimax류 채널 영상)을 소스로 쓴다. **원본은 대개 영어 나레이션이 붙어 있다** — 그 음성/스크립트를 그대로 쓰지 않고, 5번 항목의 문체로 새로 각색한다.
- 원본 영상을 여러 구간으로 잘라 하나의 타임라인에 순서대로 배치한다(컷마다 별도 소스클립일 필요는 없고, 같은 원본의 여러 구간이어도 된다).
- **★비디오 컷 수는 자막 장수보다 훨씬 적다.** 자막마다 화면을 바꾸지 않는다 — 한 화면이 자막 3~6장을 덮고 지나간다. 실측(`draft_content.json` 기준):
  | 초안 | 자막 장수 | 비디오 컷 수 | 비율 |
  |---|---|---|---|
  | 260624-b093 | 40장 | 6컷 | 자막 6.7장당 1컷 |
  | 260705-c008 | 30장 | 10컷 | 자막 3장당 1컷 |
  | 260624-b094 | 39장 | 10컷 | 자막 3.9장당 1컷 |
  - 인물형(볼케이노) 프리셋처럼 "자막 한 장 = 클립 한 개"로 만들면 이 스타일이 아니다. 장면이 바뀌는 굵직한 순간(장소 전환, 사건 전환)에서만 컷을 바꾸고, 그 사이 여러 자막은 같은 화면 위로 흘려보낸다.
- **컷 사이에 CapCut 트랜지션 효과를 넣지 않는다** — 하드컷만 쓴다. 속도감은 재생속도와 사운드로 만든다.

## 2. 캔버스 · 재생속도
- 캔버스: **1080×1920** (세로 9:16), `ratio: original`.
- 비디오 클립 재생속도: **1.0배속 또는 1.2배속을 섞어 쓴다** (`mode: 0`). 정보 전달/사건 설명 구간은 1.2배속으로 속도감을 주고, 감정이 중요한 장면(반전 직전·클라이맥스)은 1.0배속으로 늦춰 무게를 준다. 무조건 1.2배속 고정이 아니다 — 컷마다 장면의 비중을 보고 고른다.
- 클립마다 세로 위치(`clip.transform.y`)를 손으로 미세 조정한다 — 화면 중요 피사체가 자막 밴드에 가리지 않도록 클립마다 다른 값(예: -0.45, -0.05 등)을 준다. 고정 공식은 없고 컷마다 눈으로 보고 맞춘다.
- 스케일은 그대로 둔다(`scale: 1.0`, `uniform_scale.on: true`) — 확대하지 않는다.

## 3. 색보정
- CapCut 필터 **"훈훈한 포근함"**(따뜻한 톤, effect_id `7535020867344928061`)을 전체 영상에 한 번 적용, 강도 **약 0.30**(`value ≈ 0.302`).
- 추가로 클립마다 CapCut의 스마트 컬러 조정(`smart_color_adjust`, effect_id `7501974767453474064`)을 강도 **약 0.30**(`value ≈ 0.298`)으로 얹는다.
- 즉 "은은한 따뜻한 필터 + 자동 보정 약하게" 조합이며, 강하게 튀는 색보정은 쓰지 않는다.

## 4. 자막(본문 캡션) — 스타일
- 폰트: **NanumSquareRoundB.ttf**(나눔스퀘어라운드 Bold).
- 크기: 15 (CapCut 내부 스케일 기준).
- 채우기색: 흰색(`#ffffff`).
- 외곽선(stroke): 검정, 두께 ≈ 0.074.
- 그림자: 검정, 불투명도 0.9, 거리 5, 각도 -45°, 부드러움(smoothing) 0.45.
- 정렬: 가운데(`alignment: 1`), 줄간격 `line_spacing: 0.17`.
- 평균 컷당 0.6~2.0초, 한 편에 캡션 30~40장 내외.
- 나레이션(TTS) 없음 — **자막만으로 이야기를 끌고 간다.**

## 5. 자막 — 문체 (★인물형 프리셋과 가장 다른 부분)
- **명사형 종결이 아니다.** 인물형 프리셋(명사형 "~함/~음/~임")과 달리, 3D(두둥_픽)는 **입말체 완결 문장**을 그대로 쓰다가, 한 문장을 호흡 단위로 여러 자막에 걸쳐 쪼갠다. 종결어미는 "~습니다/~였죠/~했어요/~였습니다" 등 자연스러운 구어체.
- 문장을 어절 경계가 아니라 **숨 쉬는 지점**에서 자른다 — 실측 예시(260624-b093, 한 문장이 두 자막에 걸침):
  ```
  0.97s +1.47s |  수업 시간에 실수로 바지에
  2.43s +1.03s | 오줌을 싸고 말았습니다
  ```
  (뒷 자막 앞의 공백은 원본 데이터 그대로 — 한 문장을 그대로 둘로 끊었다는 흔적이다.)
- 한 자막에는 정보 하나~한 호흡만 담는다. 길어도 어절 3~6개.
- **전체 흐름은 5단 기승전결이다** (인물형의 "도입-고난-계기-성공-여운" 5단계와 유사하되 명칭과 종결어미가 다르다):
  1. **도입**: 인물과 처한 상황을 1~3문장으로 제시. ("한 소년이 수업 시간에 실수로 바지에 오줌을 싸고 말았습니다")
  2. **위기/곤란**: 그로 인한 곤란·공포·억울함이 커진다. ("이제 아무도 나와 말 섞지 않을 거라며 절망했죠" / "설상가상으로 선생님은 금붕어 관찰 실험을 하겠다며...")
  3. **전환 행동**: 다른 인물의 예상 밖 행동이나 사건이 터진다. ("친구가 물이 가득 찬 금붕어 어항을 들고 다가왔습니다 → 소년의 바지 위로 몽땅 쏟아버렸죠")
  4. **반전/진실**: 그 행동의 진짜 의미·숨은 사정이 드러난다. ("사실 나도 예전에 바지에 실수한적 있어서 그 순간이 얼마나 무서운지 잘 알아" → 어항 사고는 소년을 구하기 위한 친구의 연출이었음이 밝혀짐)
  5. **마무리/의미**: 결말과 그 사건이 남긴 관계·교훈 한 줄로 닫는다. ("그리고 이들은 단짝이 되었죠")
- 표(3편 비교) — 소재는 다양하지만 뼈대는 동일하다:

  | 초안 | 도입 | 위기 | 전환 | 반전 | 마무리 |
  |---|---|---|---|---|---|
  | b093(어항) | 소년이 바지 실수 | 관찰 실험으로 망신 예고 | 친구가 어항 물을 쏟음 | 사실은 소년을 구하려 일부러 그런 것 | 둘은 단짝이 됨 |
  | c008(양육비) | 18살 생일, 양부모가 쓰레기봉투를 줌 | 집을 나가라는 말을 들음 | 생일카드 속 연락처로 친부모를 찾음 | 친부모가 18년간 양육비를 보냈으나 양부모가 가로챔 | 양부모는 소송으로 전 재산을 잃음 |
  | b094(운명/사기) | 여자가 교통사고를 자작해 합의금을 뜯음 | 수법이 통해 대담해짐 | 페라리 운전자를 노리다 진짜 사고를 당함 | 꿈속에서 다시 걷지 못한다는 경고를 받고도 돈을 포기 안 함 | 실제로 하반신 마비 판정을 받음 |

- **영어 나레이션 원본을 다룰 때**: 원문을 그대로 번역하지 말고, 위 5단 구조에 맞춰 (1) ASR로 원문 받아쓰기 → (2) 사실관계만 뽑아 5단으로 재배열 → (3) 위 종결어미·호흡 단위로 한국어 자막을 새로 쓴다. 원문에 없는 디테일은 지어내지 않는다.

## 6. 제목(후킹 문구)
- 폰트: **Recipekorea 레코체**(손글씨 느낌의 한글 폰트).
- 크기: 14, 채우기색: 검정.
- 위치: 화면 상단, **영상 처음부터 끝까지 고정 노출**(전체 길이 동안 한 텍스트 세그먼트).
- 내용: 이야기의 결말/의미를 미리 보여주는 **2줄** 문구. 클릭베이트식으로 반전을 먼저 던지는 방식이다.
- 공식: **[사건 속 핵심 행동/대상] + [그 행동의 진짜 의미나 결과]**, 대부분 "~의도", "~최후", "~순간", "~이유" 로 끝맺는다. 실측 예:
  - "어항을 엎지른 / 친구의 진짜 의도" (행동 + 의도)
  - "양육비를 가로채던 / 양부모의 최후" (행동 + 결말)
  - "눈앞의 진실을 두고도 / 의심에 눈이 멀어버리는 순간" (상황 + 그 순간의 의미)
  - "영국의 여왕들이 / 치마를 크게 입은 이유" (사실 + 이유, 다큐형일 때)

## 7. 워터마크 / 채널 브랜딩
- 좌상단에 채널 로고 오버레이(투명 배경 PNG, 1080×1920 풀캔버스, 전체 길이 고정):
  - 검정 폭발/별 모양 배지 아이콘 + 그 안에 노란-주황 그라데이션으로 "두둥" 글자.
  - 오른쪽에 검정 글씨로 채널명 "두둥_픽" 워드마크.
- 화면 하단에 옅은 흰색→회색 그라데이션 비네트가 살짝 깔려 있다(자막 가독성 보조용으로 추정).

## 8. 오디오
- **TTS 나레이션 없음.**
- 배경음악 한 곡(약 53초 분량)을 반복 재생하지 않고, **각 자막 구간 길이에 맞춰 곡의 서로 다른 구간을 잘라 붙인다** — 즉 BGM 편집점이 자막 컷과 정확히 맞물리도록 손으로 잘게 잘라 배치한다(단순 반복 루프가 아님).
- 영상 시작 지점(0~1.8초)에 **"두둥" 드럼 히트 효과음**을 한 번 깔아 오프닝을 강하게 연다.
- 배경음악 볼륨은 1.0(별도 더킹 없음 — 나레이션이 없으므로).

## 9. 필요한 리소스 체크리스트 (다른 PC에서 재현 시)
- 폰트 설치 필요: `NanumSquareRoundB.ttf`(나눔스퀘어라운드 Bold, 눈누/네이버 배포), `Recipekorea 레코체 FONT.ttf`(레시피코리아 배포 무료 폰트).
- CapCut 내장 리소스라 그대로 복제 불가능한 것: 필터 "훈훈한 포근함"(effect_id 7535020867344928061), 스마트 컬러 조정(effect_id 7501974767453474064) — CapCut에서 이름으로 검색해 적용해야 한다.
- 채널 워터마크 PNG(`탬플릿.png`, 두둥_픽 로고): 별도로 보관해 두었다가 매 영상 오버레이 트랙에 재사용.
- 두둥 드럼 효과음(`두둥 북소리.MP3`): 별도로 보관해 재사용.

## 10. 볼케이노 파이프라인과의 관계
- 볼케이노의 `인물형` 프리셋과 뼈대(원본 영상 재활용 + 자막 스토리텔링 + 나레이션 없음)는 같지만, 다음이 다르다:
  - 인물형: 흰 캔버스 위 1080×797 비디오 + 상하 텍스트 밴드, **명사형 종결** 자막(자막 한 장 = 클립 한 개), 노란 인용구 하이라이트.
  - 3D(두둥_픽): 풀프레임 비디오(1080×1920) 위에 자막을 얹고, **입말체 완결문을 호흡 단위로 쪼갠 자막**(자막 여러 장 = 클립 한 개), 컬러 필터·워터마크·오프닝 드럼히트·가변 배속을 추가한 스타일.
- 볼케이노로 초안(대본·자막 타이밍)을 뽑을 때는 5번 항목의 문체·구조를 따르게 하고, 영상 컷은 자막마다 바꾸지 말고 1번 항목의 비율대로 묶는다. 그 위에 6~9번 항목을 CapCut에서 얹으면 "두둥_픽" 톤이 재현된다.

## 11. 나레이션(TTS) 버전 변형 — ffmpeg 직접 제작 시 실측 (2026-09-05)

기본 3D 프리셋은 나레이션이 없지만, **사용자가 특정 영상에 타입캐스트 TTS 나레이션을 요청**한 실사례에서 아래가 검증됐다. CapCut 없이 ffmpeg만으로 이 스타일을 재현할 때의 절차다.

- **영상 길이 제약**: 25~45초 범위 안에서 **45초에 가깝게** 맞추는 게 기본값이다(45초 절대 초과 금지, 짧게 끝나는 것보다 45초 근처를 채우는 쪽을 선호함— 실사용자 확인 2026-09-05). 나레이션이 있으면 자막만 있을 때보다 호흡이 느려지므로, 대본 문장 수(8~12개 안팎)로 이 길이를 맞춘다.
- **나레이션 문장은 전부 완결 어미로 끝내야 한다** — `~습니다 / ~죠 / ~어요` 중 하나로. `-고는/-지만/-며` 같은 연결어미로 끝나는 문장을 TTS 한 줄로 잘라 재생하면 말이 끊긴 것처럼 들린다. 원본 5단 구조(도입-위기-전환-반전-마무리)는 유지하되, 각 문장을 문법적으로 독립된 완결문으로 쓴다.
- **TTS 엔진**: 타입캐스트 API (`https://api.typecast.ai/v1/voices` 로 보이스 목록 조회, `POST /v1/text-to-speech` 로 합성 — 헤더 `X-API-KEY`, 바디 `{voice_id, text, model, language:"kor", prosody:{emotion_preset}, output:{audio_format:"mp3"}}`). 이번에 쓴 보이스: **"Piljae"(필재)**, `voice_id: tc_68257f68bc6e3c161ab5078d`, model `ssfm-v30`.
- **나레이션 속도**: 기본 합성 속도가 느리다고 판단되면 `ffmpeg -filter:a atempo=1.2` 로 피치 보존 배속(1.2배 실측 자연스러움 확인). 배속 후 실제 길이를 다시 재서 자막·영상 컷 타이밍을 그 값으로 다시 계산해야 한다(추측치로 계산하면 어긋난다).
- **문장 사이 간격**: 각 문장 오디오 사이에 무음 0.15초를 끼워 붙인다(`anullsrc` 로 생성 후 concat).
- **영상 컷 매핑**: 자막(=나레이션 문장) 여러 개를 한 비디오 컷이 덮는 비율(1번 항목)은 나레이션 버전에서도 유지한다. 각 컷의 소스 길이 = 그 컷이 덮는 문장들의 (나레이션 길이+간격) 합 × 그 컷의 재생속도.
- **자막 위치**: "화면 중간에서 약간 아래"가 기본 요청이었다 — 워터마크 PNG의 투명 영상창(아래 참고) 안, 세로 1920 기준 y≈1030~1100 부근(ASS `Alignment=8`, `MarginV≈1030~1100`).
- **자막을 한 줄로 강제할 때**: 문장 길이가 제각각이라 폰트를 고정 크기로 두면 길이가 넘치거나(화면 밖으로 잘림) 너무 작아진다. **문장마다 폭에 맞춰 폰트 크기를 자동 계산**한다 — PIL로 그 폰트의 실제 렌더 폭을 재서 목표 폭(예: 940px, 1080px 캔버스에 좌우 마진 70px씩)에 맞는 크기를 역산하고, 상/하한(예: 36~72px)을 둔다. ASS는 `WrapStyle: 2`(자동 줄바꿈 끔) + 각 Dialogue 줄 앞에 `{\fs<계산된크기>}` 오버라이드 태그를 넣는다. 자막 문구는 나레이션 문장을 그대로 쓴다(임의로 축약하지 않는다) — 축약이 필요하면 사용자에게 먼저 확인한다.
- **배경음악**: 나레이션이 있는 편집에서는 "배경음악 없이 나레이션+두둥 효과음만" 요청을 받았다 — 즉 나레이션이 있을 땐 BGM이 필수가 아니다. 두둥 드럼 효과음은 오프닝(0~1.8초)에 깔고, 필요시 `volume=+6~8dB` 로 키운 뒤 `alimiter=limit=0.95` 로 클리핑을 막는다. amix 사용 시 `normalize=0` 을 줘야 자동으로 반씩 깎이지 않는다.
- **영상 오버레이 구조 실측**(두둥_픽 워터마크 PNG, 1080×1920 풀캔버스 기준):
  - y 0~488: 불투명 흰 배경(로고+제목 자리)
  - y 488~1498 (높이 1010): **완전 투명 — 실제 영상이 들어가는 창**
  - y 1498~1920: 불투명 연회색 그라데이션(원래 디자인은 이 자리도 카드/자막용으로 보이나, 이번 요청에서는 자막을 영상창 쪽으로 올렸다)
  - ffmpeg 재현: 원본 클립을 `crop=1080:1010:0:455`(세로 중앙 1010px만 취함) → `pad=1080:1920:0:488`(투명창 위치로 이동) → 워터마크 PNG를 `overlay=0:0` → 마지막에 `ass=` 필터로 자막 번인.
- **소제목(후킹 타이틀) 위치 — 실제 화면 캡처로 검증됨(2026-09-05)**: CapCut 프로젝트 JSON의 `transform.y` 값만 보고 "아래쪽 회색 카드 영역"으로 추정했다가 **틀렸다** — 실제 재생 화면을 캡처해 보니 소제목은 **로고 바로 아래, 상단 흰 배경 영역**에 들어간다(JSON 좌표계 역산은 신뢰하지 말고, 가능하면 실제 프리뷰/재생 화면 스크린샷으로 검증할 것).
  - 위치: `Alignment=8`(상단), `MarginV≈250~260` — 로고 아이콘/워드마크와 겹치지 않을 만큼만 아래로 내린다.
  - 크기: 두 줄이 **같은 크기가 아니다** — **아래 줄이 위 줄보다 약 13% 크다**(`size2 ≈ size1 × 1.13`). 폭에 맞춰 자동 축소(위 자막과 같은 PIL fit 방식)로 기준 크기를 구한 뒤 이 비율을 적용한다.
  - **크기는 크게 갈수록 좋다는 피드백을 받았다(2026-09-05)** — 목표 폭을 캔버스에 거의 꽉 차게(≈1040px, 캔버스 1080px 중), 상한을 90→**110px**로 올렸다(실측 결과 77px/88px). 다음에도 작아 보이면 목표 폭·상한을 더 올릴 것.
  - 폰트: Recipekorea Medium, 색상 검정, 테두리/그림자 없음.
  - 워터마크 PNG의 "투명 영상창" 시작(y=488)보다 한참 위(y≈250~330 부근)에서 끝나야 하며, 그 사이 여백은 흰 배경 그대로 둔다.

## 12. 타사(유튜브 등) 소스 영상 재가공 시 실측 (2026-09-06)

- **소스에 이미 박제된 캡션/워터마크는 Vmake로 지워도 100% 지워지지 않는다.** 특히 원본 채널의 네온/키워드 강조 캡션("GRABBED HER" 처럼 단어 단위로 팝업하는 스타일)은 `videoscreenclear`(target: text_watermark)로도 잔여물이 남을 수 있다. **매번 완벽 제거를 기대하지 말고, 새 자막을 원본 자막이 있던 자리에 그대로 배치해서 덮는다.**
  - 원본 잔여 캡션 위치는 실측(여러 프레임 스크린샷 비교)으로 찾는다 — 이번 소스는 크롭 전 좌표 기준 y≈900~1000(1920 기준) 부근에 고정 위치로 나타났다(장면이 바뀌어도 위치는 동일, 텍스트만 바뀜 — 이런 영상 장르의 공통 관례로 보임).
  - crop/pad 매핑 공식: `canvas_y = source_y - crop_offset_y + pad_offset_y`. 이번 값(`crop=1080:1010:0:455`, `pad=...:0:488`)에서는 `canvas_y = source_y + 33`.
  - **불투명 박스는 쓰지 않는다** — 사용자가 명시적으로 거부했다(2026-09-06, "불투명 검정 박스는 넣으면 안돼"). 배경 박스 없이, 새 자막 자체를 크게(목표 폭 ≈1000px, 상한 84px 등 기존값보다 키움) + 외곽선/그림자를 두껍게(`Outline≈5`, `Shadow≈4`) 해서 잔여 텍스트 위에 바로 얹는 것으로 가린다. `\an5\pos(540, 중앙y)`로 그 위치에 중앙 정렬. 박스 없이 글자만으로 가리는 방식이라 100% 커버는 아니지만, 이게 이 스타일이 허용하는 기본값이다.

- **나레이션-자막 불일치 버그와 재발 방지책**: TTS 생성 스크립트(`tts_gen.py` 류)가 `if not os.path.exists(mp3_path): ...생성...` 패턴으로 캐싱하면, **대본을 나중에 수정해도 이미 존재하는 이전 버전의 mp3 파일이 재사용되어** 자막 텍스트와 실제 나레이션 음성이 달라지는 사고가 난다(실제로 발생: 4번째 문장부터 밀려서 엉뚱한 문장이 재생됨). 대본을 확정하기 전까지는 mp3 캐시를 신뢰하지 말 것 — **대본이 바뀌면 관련 TTS mp3를 전부 삭제하고 재생성**하거나 애초에 캐싱 없이 매번 새로 생성한다.
- **자막-나레이션 정밀 동기화**: 어절 개수 비례 배분(글자수/단어수로 균등 분할)은 TTS 내부의 불균일한 쉼표 휴지(문장 중간의 자연스러운 pause)를 반영하지 못해 자막이 실제 발화보다 앞서거나 늦게 나타날 수 있다. **Speechmatics ASR(`POST /v2/jobs`, language: "ko")로 각 문장 TTS mp3의 단어별 타임스탬프(`format=json-v2`)를 직접 뽑아**, 인식된 단어 개수가 원본 어절 개수와 일치하면 그 타임스탬프를 그대로 자막 구간 경계로 쓴다(개수가 다르면 — 숫자 표기나 붙여쓰기로 인식이 갈릴 때 — 글자수 비례 분할로 폴백). 문장의 마지막 자막 조각은 다음 문장 시작 전 무음 구간(0.15초 패딩)까지 늘려서 자막이 잠깐 사라지는 깜빡임을 막는다.
- **최종 확인(2026-09-06)**: 위 내용을 모두 반영한 v3 결과물을 사용자가 검수 후 확정("확인했어. 굿~~"). 이 워크플로를 그대로 구현한 실제 파이썬 스크립트를 `scripts/narration_variant/`에 보관한다 — `tts_gen.py`(타입캐스트 TTS 생성+배속) → `asr_align.py`(Speechmatics 단어 정렬) → `build_segments.py`(컷별 배속/컨캣) → `build_audio.py`(나레이션+두둥 사운드 믹스) → `build_ass.py`(자막/제목 ASS 생성, 박스 없이 폰트 확대 방식) → `render_video.py`(크롭/색보정/워터마크/자막 합성) 순서로 실행한다. 문장 대본(`LINES`/`FRAGMENTS`/`TITLE_LINES`)은 영상마다 바뀌므로 이 스크립트들은 그대로 재실행하지 말고 다음 영상의 대본에 맞게 값만 교체해서 참고용으로 쓴다.

## 13. 문장별 영상 구간 선정 — 인물·시점 정밀 매칭 (2026-09-06)

- **나레이션 문장의 주어(등장인물)와 화면에 나오는 얼굴이 일치해야 한다.** "사장님이 백 달러를 건넸다"는 문장에 종업원 얼굴이 나오면 안 된다 — 사용자 지적(2026-09-06): "영상 포인트는 나래이션에 나오는 인물 중심으로 만들면돼... 다시 만들라는 말 아니고 기억하라는거야."
- 실무 절차: (1) 원본 영상 오디오를 Speechmatics ASR(`language:"en"`, `format=json-v2`)로 단어 단위 타임스탬프까지 받는다 → (2) 대본 각 문장의 핵심 사건에 해당하는 원본 대사의 앵커 단어(인물 이름 대신 금액·장소·행동 키워드 등)를 찾아 그 구간의 시작/끝 시각을 특정한다 → (3) 그 구간에서 실제로 화면에 잡히는 인물이 문장의 주체와 일치하는지 프레임을 뽑아 눈으로 확인한다(썸네일 여러 장 비교) → (4) 문장 하나당 영상 구간 하나(1문장=1컷)로 정밀 매칭해 자른다. 기본 3D 스타일의 "자막 여러 장 = 컷 하나"(1번 항목) 원칙은 나레이션 버전에서는 인물 매칭 정확도를 위해 깨질 수 있다 — 정확도가 우선이다.
- 문장별 구간 길이(배속 적용 전 raw)는 그 문장의 TTS 길이(+0.15초 패딩)에 배속(0.7~1.9배 범위, 감정 장면은 느리게)을 역산해서 정한다: `raw_길이 = 목표_나레이션_길이 × 배속`.

## 14. 잔여 자막 제거 — 크롭 위치로 무료로 가리기 (2026-09-06)

- 두둥픽 워터마크 템플릿은 캔버스(1080×1920) 기준 y 0~488이 불투명 흰색, y 1498~1920이 불투명 회색, 그 사이 y 488~1498(높이 1010)만 투명하다. 렌더 파이프라인은 **원본(1920 높이)에서 1010px만 크롭**해 이 투명창에 채워 넣는다 — 즉 크롭 범위 밖의 원본 내용은 애초에 화면에 나오지 않는다(지우는 게 아니라 아예 안 보여준다).
- **사용자 지적(2026-09-06)**: "영상 템플릿이 위 아래로 가릴 수 있게 되어 있잖아. 자막이 아래쪽에 있을 때는 자막이 가려질 수 있을 만큼 내려서 배치하면 돼. 그러면 Vmake는 안 써도 되는 거지. 자막이 가운데 있을 때만 Vmake를 쓰면 돼." — 원본에 박제된 자막이 화면 아래쪽에 있으면, **크롭 오프셋을 조정해서 그 부분이 크롭 범위 밖으로 빠지게** 하면 Vmake 없이도 완전히 안 보이게 할 수 있다. 원본 자막이 진짜 화면 정중앙(인물과 겹치는 위치)에 있어서 크롭으로 피할 수 없을 때만 Vmake가 필요하다.
- **기본 크롭 위치를 정중앙(`offset=(1920-1010)/2=455`)이 아니라 상단 쪽으로 옮겨 쓴다** — 사용자 지적: "모든 원본영상 배치할 때는 중심부가 아니고 상단 부분을 쓰면 돼." 이런 AI 합성 숏폼 장르는 인물 얼굴이 화면 위~중간에 오고 박제 자막/키워드는 화면 아래쪽에 깔리는 경우가 많으므로, 크롭 오프셋을 기본값보다 낮은 값(실측 사례: 455→380)으로 낮춰 상단 위주로 잘라내면 하단의 잔여 자막이 자동으로 크롭 밖으로 빠진다.
- 절차: 소스 영상에서 잔여 자막이 보이는 프레임 몇 장을 뽑아 자막의 y좌표(1920 기준)를 확인 → 크롭 오프셋+크롭높이(1010)가 그 y좌표보다 작아지도록 오프셋을 낮춘다 → 렌더 후 다시 프레임을 뽑아 (a) 잔여 자막이 사라졌는지 (b) 인물 얼굴(특히 턱)이 잘리지 않았는지 확인한다. 인물 클로즈업이 많은 영상은 오프셋을 너무 낮추면 턱이 잘릴 수 있으니 프레임으로 반드시 검증할 것.
- 이 방식은 Vmake API 크레딧을 전혀 쓰지 않는다 — **항상 이 방법을 먼저 시도하고, 화면 정중앙에 박제된 자막만 Vmake로 처리**하는 순서를 기본으로 삼는다.
- 크롭 오프셋은 영상 전체에 한 번(전역)만 정해도 되고, 필요하면 사용자 요청으로 더 조정할 수 있다(실측: 455→380→300, 매번 얼굴이 안 잘렸는지 프레임으로 재확인하며 더 올림/내림).

## 15. 대본 문장 — 모호한 서술 금지, 구체적 사실을 명시 (2026-09-06)

- **스토리의 핵심 "팩트"(누가 무엇을 어떻게 했는지)를 뭉뚱그려 쓰면 안 된다.** 사용자 지적: "스토리에 여자가 망신줄때가 팩트인데 어떻게 망신 준건 모두 빠져버렸어. 모든 대본은 시청자가 알아 듣기 쉽게 구성해야해."
- 실패 사례: "여자는 갑자기 큰 소리로 그를 이상한 사람 취급했어요"(추상적, 정작 여자가 뭐라고 소리쳤는지 안 나옴) → 수정: "여자는 다짜고짜 큰 소리로 그가 함께 밤을 보내자고 했다며 몰아붙였어요"(여자의 구체적 발언 내용을 명시).
- 구체적으로 쓰면 뒷부분과의 연결(복선-회수 구조)도 저절로 살아난다 — 위 예시에서 남자가 나중에 "하룻밤에 20달러"라고 되받아치는 반전이, 앞에서 여자가 뭐라고 몰아붙였는지 명시했을 때만 왜 통쾌한 반전인지 이해된다.
- 원문 번역이 아니라 사실 재구성이라는 원칙(5번 항목)은 유지하되, **"사실을 뭉개서 요약"하지 말고 "사실을 내 표현으로 구체적으로" 쓴다** — 즉 원문의 정확한 워딩을 베끼지 않으면서도, 인물이 실제로 한 말/행동의 핵심 내용(숫자, 발언 요지, 결정적 디테일)은 반드시 살려서 쓴다.
- 대본 한 문장을 수정하면 해당 문장의 TTS 길이가 바뀌므로, 그 문장이 속한 영상 세그먼트의 배속도 다시 계산해야 한다(`raw_길이 = 목표_나레이션_길이 × 배속`, 13번 항목). 전체 영상 길이가 나레이션 총 길이보다 짧아지지 않도록 — 오디오가 잘리면 안 되므로 — 최종 concat 길이가 오디오 길이 이상인지 반드시 재확인한다.

## 16. 제목 공식 — 상황별 하위 패턴 선택 (2026-09-06)

- 사용자가 다른 채널의 제목 약 120개를 그대로 제공하여 분석을 요청했다. 공통 공식: **[짧은 인물/상황 수식어구] + (구체적 숫자·디테일, 자주 등장) + [결과를 예고만 하고 숨기는 종결어]**. 결말 자체는 절대 먼저 말하지 않고 "왜/어떻게"라는 물음표만 남긴다.
- 상황에 따라 아래 하위 패턴 중 그 영상 스토리 성격에 맞는 것을 고른다(사용자 확인: "상황에 따라 제목 맞춰서 만들 수 있겠어?" → 그렇게 하기로 함):
  - **반전 대비형**(복수/사이다물): "A했던 사람이 / B당한 순간·굴욕" — 가해자를 먼저 세우고 그의 반전을 예고.
  - **진실/정체형**(미스터리 반전물): "~의 정체", "~에서 밝혀진 진실/비밀".
  - **최후형**(권선징악물): "~의 최후".
  - **방법형**(해결책 제시물): "~하는 법", "~한 방법".
  - **이유형**(범용 기본값): "~한 이유" — 다른 패턴이 안 맞을 때 쓰는 가장 무난한 선택.
- 실측 예(망신당한 남자 영상): "큰소리로 망신 준 여자가 / 5분 뒤 당한 굴욕"(반전 대비형 채택).
